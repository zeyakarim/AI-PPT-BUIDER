import { PageContainer } from "../../../components/PageContainer";

export const ContentArea = () => {
  return (
    <main className="relative box-border caret-transparent basis-[0%] grow z-0 overflow-auto">
      <PageContainer />
    </main>
  );
};
